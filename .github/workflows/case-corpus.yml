name: Refresh Case Intelligence Corpus

on:
  workflow_dispatch:
    inputs:
      limit:
        description: 'Candidate corpus size (500-1000)'
        required: false
        default: '750'
  schedule:
    - cron: '17 3 * * 1'
  push:
    branches: [main]
    paths:
      - 'scripts/import-case-candidates.mjs'
      - 'scripts/augment-case-candidates.mjs'
      - 'scripts/refresh-case-corpus.mjs'
      - 'scripts/build-case-review-queue.mjs'
      - 'scripts/build-coverage-plan.mjs'
      - 'studio/coverage-planner-engine.js'
      - 'studio/case-intelligence.js'
      - 'studio/case-intelligence-runtime.js'
      - 'studio/multi-source-index.js'
      - '.github/workflows/case-corpus.yml'

permissions:
  contents: write

concurrency:
  group: case-corpus-${{ github.ref }}
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Refresh corpus, strategic review queue and coverage plan
        env:
          CORPUS_LIMIT: ${{ inputs.limit || '750' }}
        run: node scripts/refresh-case-corpus.mjs --limit "$CORPUS_LIMIT" --min 500 --queue 90 --backlog 30 --output studio/case-candidates.json --queue-output studio/case-review-queue.json --plan-output studio/coverage-plan.json

      - name: Report corpus and coverage progress
        run: |
          node --input-type=module - <<'NODE'
          import { appendFile, readFile } from 'node:fs/promises';
          const corpus = JSON.parse(await readFile('studio/case-candidates.json', 'utf8'));
          const queue = JSON.parse(await readFile('studio/case-review-queue.json', 'utf8'));
          const plan = JSON.parse(await readFile('studio/coverage-plan.json', 'utf8'));
          const candidates = corpus.stats?.candidates || 0;
          const queueSize = queue.stats?.queue || 0;
          const target = corpus.target?.min || 500;
          const sourceStats = corpus.sourceStats || [];
          const top = (plan.collections || []).slice(0, 8).map(item => `${item.title}: ${item.priority} (${item.nextAction})`).join('; ');
          const summary = [
            '## Research Corpus + Coverage Planner refresh',
            '',
            `- Candidates: **${candidates} / ${target} minimum target**`,
            `- Target reached: **${candidates >= target ? 'yes' : 'no — partial snapshot published'}**`,
            `- Strategic deep-review queue: **${queueSize}**`,
            `- Curated cases considered by queue: **${queue.stats?.curatedCasesConsidered || 0}**`,
            `- Queue source pools represented: **${queue.stats?.sourcePoolsRepresented || 0}**`,
            `- Critical Collections: **${plan.summary?.criticalCollections || 0}**`,
            `- High-priority Collections: **${plan.summary?.highPriorityCollections || 0}**`,
            `- Planner backlog: **${plan.backlog?.length || 0}**`,
            `- Source pools responding: **${sourceStats.filter(item => item.ok).length}/${sourceStats.length}**`,
            `- Automated pools: **${sourceStats.map(item => item.source).join(', ')}**`,
            '',
            `Top priorities: ${top || 'none'}`,
            '',
            candidates < target ? '> Partial data is intentionally published so the research/planning UI remains useful while discovery coverage is expanded.' : '> Minimum corpus target reached.'
          ].join('\n');
          if (process.env.GITHUB_STEP_SUMMARY) await appendFile(process.env.GITHUB_STEP_SUMMARY, `${summary}\n`);
          console.log(JSON.stringify({ target, targetReached: candidates >= target, corpus: corpus.stats, queue: queue.stats, planner: plan.summary, sourceStats }, null, 2));
          NODE

      - name: Commit refreshed snapshots
        run: |
          if git diff --quiet -- studio/case-candidates.json studio/case-review-queue.json studio/coverage-plan.json; then
            echo 'Research/coverage snapshots unchanged.'
            exit 0
          fi
          git config user.name 'seedance-porter-bot'
          git config user.email 'seedance-porter-bot@users.noreply.github.com'
          git add studio/case-candidates.json studio/case-review-queue.json studio/coverage-plan.json
          git commit -m 'data: refresh Case Intelligence research and coverage snapshots'
          git push origin HEAD:main
